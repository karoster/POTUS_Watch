<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <p>please click the following link to set up a live email alert when the president tweets the following key words:</p>
    <ul>
        <li>
          <% @unverified_alert.words.each do |word| %>
              <li><%= word %></li>
          <% end %>
        </li>
    </ul>
    <p>
      <a href=<%="localhost:3000/api/v1/#{@unverified_alert.id}/verify?verification_token=#{@unverified_alert.verification_token}"%>>
        Click HERE!
      </a>

    </p>
  </body>
</html>